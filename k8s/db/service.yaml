apiVersion: v1
kind: Service
metadata:
  name: casino-db
  namespace: vidalcasino
spec:
  type: ClusterIP
  selector:
    app: casino-db
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP