apiVersion: v1
kind: Service
metadata:
  name: casino-backend
  namespace: vidalcasino
spec:
  type: ClusterIP
  selector:
    app: casino-backend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP